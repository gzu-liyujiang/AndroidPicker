apply plugin: 'com.android.library'

ext {
    isLibrary = true
    pomArtifactId = "Toolkit"
    pomVersion = "2.0.0"
    pomDescription = "Toolkit of android picker"
}

dependencies {
    implementation "com.android.support:support-compat:${ANDROID_SUPPORT_VERSION}"
}
