apply plugin: 'com.android.library'

ext {
    isLibrary = true
    pomArtifactId = "AndroidPicker"
    pomDescription = "Picker For Android, include WheelPicker, ColorPicker, FilePicker and so on"
}

dependencies {
}
